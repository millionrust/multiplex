[package]
name = "multiplex-screen-session"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
description = "Sans-IO host and viewer session state machines for TermiRust Remote Screens"
license = "MIT OR Apache-2.0"

[dependencies]
multiplex-screen-codec = { version = "0.1.0", path = "../multiplex-screen-codec" }
multiplex-screen-protocol = { version = "0.1.0", path = "../multiplex-screen-protocol" }
multiplex-screen-video = { version = "0.1.0", path = "../multiplex-screen-video" }
